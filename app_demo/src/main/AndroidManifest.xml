<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leo.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Menu1" />
        <activity android:name=".Menu2" />
        <activity android:name=".Menu3" />
        <activity android:name=".SimpleAnimation" />
        <activity android:name=".DialogAndThemeChange" />
        <activity android:name=".SimpleIOStorage"></activity>
        <activity android:name=".SqliteTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>